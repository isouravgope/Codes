#include <stdio.h>
#include <stdlib.h>


int main ()
{
int age;
int gpa;
printf ("enter the required age and gpa");
scanf("%d", &age);
scanf("%d", &gpa);
if (age <= gpa ){printf ("true");}
else {printf ("false");}


}